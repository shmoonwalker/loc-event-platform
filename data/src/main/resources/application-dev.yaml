logging:
  level:
    root: INFO
    nl.loc.data: DEBUG

# Object storage settings are inherited from application.yaml.
# Storage is disabled by default; set R2_ENABLED=true plus the R2_* variables
# to use the real development bucket.
