# Development scripts

These scripts manage the active local application stack:

- `dev-up.sh` starts PostgreSQL, the backend, and the frontend.
- `dev-down.sh` stops the local Compose stack.
- `db-setup.py` prepares the local PostgreSQL schemas and roles when needed.

The Java data application is developed and tested from [`../data`](../data).
